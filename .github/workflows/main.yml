name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: macOS-latest
    steps:
    - name: Run a one-line script
      run: |
        echo Hello, world!
        cd ./hpsocket
        ls
        cmake .
        ls
        make
      working-directory: .

    - uses: actions/checkout@v2
