name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  commit_comment:
    branches: [ master ]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Run a one-line script
      uses: actions/checkout@v2
    - name: build
      run: |
        echo Hello, world!
        # cd ./hpsocket
        cd ../..
        dir
        # cmake .
        # ls
        # make
